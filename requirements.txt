streamlit
google-genai
pandas
gTTS

# Optional upgrades — the app auto-detects these and enables extra features
# if installed. It works fine without them (with graceful fallbacks), but
# uncomment for the full experience:
# audio-recorder-streamlit   # enables live in-browser mic recording
# streamlit-geolocation      # enables one-tap GPS location capture
